services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: raiden-shin-postgres
    environment:
      POSTGRES_USER: raiden
      POSTGRES_PASSWORD: raiden
      POSTGRES_DB: raiden_shin_boot
    ports:
      - "5432:5432"
    volumes:
      - raiden_shin_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U raiden -d raiden_shin_boot"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis/redis-stack-server:latest
    container_name: raiden-shin-redis
    ports:
      - "6379:6379"
    volumes:
      - raiden_shin_redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10

  migrate:
    build:
      context: .
      dockerfile: packages/server/Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: postgres://raiden:raiden@postgres:5432/raiden_shin_boot
    command: ["pnpm", "db:migrate"]
    depends_on:
      postgres:
        condition: service_healthy
    profiles:
      - app
      - migrate

  api:
    build:
      context: .
      dockerfile: packages/server/Dockerfile
    env_file:
      - .env
    environment:
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: 8787
      DATABASE_URL: postgres://raiden:raiden@postgres:5432/raiden_shin_boot
      REDIS_URL: redis://redis:6379
    ports:
      - "${SERVER_PORT:-8787}:8787"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    profiles:
      - app

  panel:
    build:
      context: .
      dockerfile: packages/panel/Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8787}
    ports:
      - "${PANEL_PORT:-5173}:80"
    depends_on:
      - api
    profiles:
      - app

  bot:
    build:
      context: .
      dockerfile: packages/bot/Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: postgres://raiden:raiden@postgres:5432/raiden_shin_boot
      REDIS_URL: redis://redis:6379
      BOT_RUNTIME_MODE: polling
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    profiles:
      - bot

  bot-worker:
    build:
      context: .
      dockerfile: packages/bot/Dockerfile
    env_file:
      - .env
    environment:
      DATABASE_URL: postgres://raiden:raiden@postgres:5432/raiden_shin_boot
      REDIS_URL: redis://redis:6379
      BOT_RUNTIME_MODE: worker
      BOOT_MEMORY_ENRICHMENT_ASYNC_ENABLED: "true"
    command: ["pnpm", "--filter", "@raiden/bot", "start:worker"]
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      migrate:
        condition: service_completed_successfully
    profiles:
      - worker

volumes:
  raiden_shin_postgres:
  raiden_shin_redis:
